<div class='mt-4'>
    <div class='row'>
        <div class='col-lg-3'>
            <button>Học ngay</button>
        </div>
        <div class='col-lg-9'>
            <h2> {{course.name}}</h2>
            <img class='image' src='{{course.image}}' alt='' />
            <p>{{course.description}}</p>
        </div>
    </div>
</div>